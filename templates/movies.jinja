<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Is it on Netflix</title>
</head>
<body>
  {% if is_some %}
    <h1>Your list is bing prepared</h1>
  {% else %}
  <h1>Your titles are ready!</h1>
  <ul>
    {% for titel in titles %}
      <li>{{titel}}</li>
    {% endfor %}
  </ul>
  {% endif %}
</body>
</html>
